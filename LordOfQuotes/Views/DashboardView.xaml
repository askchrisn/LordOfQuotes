<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:LordOfQuotes.ViewModels"
    xmlns:templates="clr-namespace:LordOfQuotes.Views.Templates"
    x:Class="LordOfQuotes.Views.DashboardView"
    x:Name="DashboardViewPage">

    <StackLayout Padding="10">
        <Button Text="Joker" BackgroundColor="Black" Command="{Binding ButtonClickedCommand}"></Button>

        <ScrollView>
        <StackLayout BindableLayout.ItemsSource="{Binding Quotes}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <templates:QuoteTemplate ParentContext="{Binding BindingContext, Source={x:Reference DashboardViewPage}}"/>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
        </ScrollView>

        <StackLayout Orientation="Horizontal" VerticalOptions="EndAndExpand" HorizontalOptions="FillAndExpand">
            <Button Text="Left" Command="{Binding PageDownCommand}"></Button>
            <Button Text="{Binding Pagination}"></Button>
            <Button Text="Right" Command="{Binding PageUpCommand}"></Button>
        </StackLayout>

    </StackLayout>

</ContentPage>
